apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - base/tenant-tiers.yaml
  - team-a/tenant.yaml
# One line added per new team. That line is the entire provisioning request.
